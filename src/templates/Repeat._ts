[1, 2, 3, 4, 5].map((val, i, arr) => {
    if (0 === i) {
        // first one
    }
    if (arr.length - 1 === i) {
        // last one
    }
    if (i % 2 == 0) {
        // even
    }
    if (i % 2 != 0) {
        // odd
    }
});